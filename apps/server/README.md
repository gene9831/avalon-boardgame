# `@avalon/server`

This package is the future Node.js entrypoint for the boardgame.io server, Socket.IO transport, lobby flow, and PostgreSQL storage adapter.

The workspace migration only establishes the package boundary. Game rules, server authority, reconnect handling, and persistence are implemented in later steps.
